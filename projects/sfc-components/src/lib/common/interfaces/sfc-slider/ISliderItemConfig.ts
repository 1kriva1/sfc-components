import { Type } from "@angular/core";
import { ComponentSize, PositionType } from "../../constants/common-constants";

export default interface ISliderItemConfig {
    title?: string;
    subTitle?: string;
    imgSrc: any;
}